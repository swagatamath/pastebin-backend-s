# Database connection string
# For local development, use a local PostgreSQL instance
# For Vercel deployment, this should be set as an environment variable in Vercel dashboard
DATABASE_URL=postgresql://username:password@localhost:5432/pastebin

# Optional: Set to "1" to enable test mode
TEST_MODE=0

# Environment (automatically set by Vercel)
# NODE_ENV=production
# VERCEL_ENV=production